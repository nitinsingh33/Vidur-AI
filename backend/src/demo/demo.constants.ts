/**
 * Every record created by the judge-facing detection demo is tagged with
 * this externalId prefix so `DemoService.reset()` can find and remove
 * exactly those records — and nothing else — for the authenticated merchant.
 */
export const DEMO_EXTERNAL_ID_PREFIX = 'VIDUR-DEMO-';

export const DEMO_CUSTOMER_EXTERNAL_ID = 'VIDUR-DEMO-CUSTOMER';
